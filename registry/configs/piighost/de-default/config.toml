schema_version = 1

[config]
name = "de-default"
description = { en = "Germany without a model: phone and postal code, plus the European IBAN and VAT number, plus the email, URL, IPv4 and card number that belong to no country. Word-boundary expansion catches a value detected once everywhere else in the message.", fr = "Allemagne sans modèle : téléphone et code postal, plus l IBAN et le numéro de TVA européens, plus l e-mail, l URL, l IPv4 et le numéro de carte qui n appartiennent à aucun pays. L expansion par mot entier reprend partout dans le message une valeur détectée une fois." }
tags = ["chat", "customer-support", "de", "document", "eu"]
piighost = ">=1.7,<2"

[[detectors]]
name = "regex-de"
type = "regex"
groups = ["piighost/de"]

[[detectors]]
name = "regex-eu"
type = "regex"
groups = ["piighost/eu-extended"]

[[detectors]]
name = "regex-generic"
type = "regex"
groups = ["piighost/generic"]

[stages.linker]
type = "exact"

[stages.expander]
type = "word_boundary"

[stages.anonymizer.placeholder]
type = "label_counter"
