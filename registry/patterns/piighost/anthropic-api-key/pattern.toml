schema_version = 1

[pattern]
name = "anthropic-api-key"
label = "ANTHROPIC_API_KEY"
tags = ["international", "secrets", "software-dev"]
regex = '\bsk-ant-(?:api|admin)\d{2}-[A-Za-z0-9_-]{80,120}(?![A-Za-z0-9_-])'

[pattern.description]
en = "Anthropic API key: the sk-ant-api user prefix and the sk-ant-admin admin prefix, the two version digits left open so a later rotation still matches. Noise is very low, the sk-ant- prefix belongs to no one else, and the loose bound of eighty to a hundred and twenty key characters is the only source of over-matching. Collision: the existing openai-api-key swallowed the identical span until its regex was given the (?!ant-) exclusion, which labelled every Anthropic key OPENAI_API_KEY. Keep that exclusion in place, or the two patterns fight over the same span again."
fr = "Clé d'API Anthropic : le préfixe utilisateur sk-ant-api et le préfixe d'administration sk-ant-admin, les deux chiffres de version laissés libres pour qu'une rotation future reste reconnue. Le bruit est très faible, le préfixe sk-ant- n'appartient à personne d'autre, et la borne large de quatre-vingts à cent vingt caractères de clé est la seule source de sur-détection. Collision : le motif existant openai-api-key avalait exactement le même span avant que son expression ne reçoive l'exclusion (?!ant-), ce qui étiquetait toute clé Anthropic en OPENAI_API_KEY. Cette exclusion doit rester, sinon les deux motifs se disputent à nouveau le même span."

# Clé inventée, de la bonne forme, jamais émise.
[[examples.match]]
text = "ANTHROPIC_API_KEY=sk-ant-api03-EFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ012345678AA"
value = "sk-ant-api03-EFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ012345678AA"

# Clé d'administration inventée.
[[examples.match]]
text = "La cle admin sk-ant-admin01-JKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdAA a fuite dans un journal."
value = "sk-ant-admin01-JKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdAA"

# Trop court pour être une clé.
[[examples.no_match]]
text = "sk-ant-api03-tooshort"

# Une clé OpenAI : elle a son propre motif.
[[examples.no_match]]
text = "sk-proj-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"

[redos]
prefix = "sk-ant-api03-"
filler = "a"
suffix = "!"
