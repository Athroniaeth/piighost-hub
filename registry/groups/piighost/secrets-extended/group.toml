schema_version = 1

[group]
name = "secrets-extended"
description = { en = "Every credential shape the registry knows: the four of the `secrets` group plus Anthropic, Google, Slack, Twilio, SendGrid, Azure, npm, PyPI, the AWS secret key, JWTs, bearer tokens and PEM private key blocks. Several of these contain long digit runs, so a phone or card pattern in the same detector claims part of them: declare this group first, or keep it in a detector of its own.", fr = "Toutes les formes de secret que le registre connaît : les quatre du groupe `secrets` plus Anthropic, Google, Slack, Twilio, SendGrid, Azure, npm, PyPI, la clé secrète AWS, les JWT, les jetons bearer et les blocs de clé privée PEM. Plusieurs contiennent de longues suites de chiffres, donc un motif de téléphone ou de carte placé dans le même détecteur en réclame une partie : déclarez ce groupe en premier, ou gardez-le dans un détecteur à part." }
tags = ["international", "secrets", "software-dev", "code-assistant", "logs"]

# Longest and most distinctive first: a PEM block and a JWT carry the widest
# spans, so they take their ground before anything narrower is tried.
[[sources]]
ref = "piighost/private-key-block"

[[sources]]
ref = "piighost/jwt"

[[sources]]
ref = "piighost/secrets"

[[sources]]
ref = "piighost/anthropic-api-key"

[[sources]]
ref = "piighost/google-api-key"

[[sources]]
ref = "piighost/slack-token"

[[sources]]
ref = "piighost/aws-secret-access-key"

[[sources]]
ref = "piighost/azure-storage-key"

[[sources]]
ref = "piighost/sendgrid-api-key"

[[sources]]
ref = "piighost/twilio-sid"

[[sources]]
ref = "piighost/npm-token"

[[sources]]
ref = "piighost/pypi-token"

[[sources]]
ref = "piighost/bearer-token"
