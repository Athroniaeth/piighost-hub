schema_version = 1

[group]
name = "secrets-extended"
description = { en = "Every credential shape the registry knows: the four of the `secrets` group plus Anthropic, Google, Slack, Twilio, SendGrid, Azure, npm, PyPI, GitLab, Mailgun, DigitalOcean, the Google OAuth client id, the AWS secret key, JWTs, bearer tokens, SSH public keys, PEM private key blocks and PEM certificate blocks, and the three credentials that live inside a URL: a Slack webhook, a database URL and HTTP basic auth. A certificate is here because it carries the subject's name, organisation and often an email address in clear text. Several of these contain long digit runs, so a phone or card pattern in the same detector claims part of them: declare this group first, or keep it in a detector of its own. The URL-shaped three start where a URL starts and must be declared ahead of it, which is why the `logs` group leaves URL out entirely.", fr = "Toutes les formes de secrets que connaît le registre : les quatre du groupe `secrets` plus Anthropic, Google, Slack, Twilio, SendGrid, Azure, npm, PyPI, la clé secrète AWS, les JWT, les jetons bearer, les blocs de clé privée PEM et les blocs de certificat PEM. Un certificat figure ici parce qu'il porte en clair le nom du sujet, son organisation et souvent une adresse e-mail. Plusieurs contiennent de longues suites de chiffres, donc un motif de téléphone ou de carte dans le même détecteur en revendique une partie : déclarez ce groupe en premier, ou gardez-le dans un détecteur à part." }
tags = ["international", "secrets", "software-dev", "code-assistant", "logs"]

# Longest and most distinctive first: a PEM block and a JWT carry the widest
# spans, so they take their ground before anything narrower is tried. The three
# URL-shaped credentials come with them, ahead of anything a URL could claim.
[[sources]]
ref = "piighost/private-key-block"

[[sources]]
ref = "piighost/slack-webhook"

[[sources]]
ref = "piighost/db-connection-string"

[[sources]]
ref = "piighost/basic-auth-url"

[[sources]]
ref = "piighost/jwt"

[[sources]]
ref = "piighost/secrets"

[[sources]]
ref = "piighost/anthropic-api-key"

[[sources]]
ref = "piighost/google-api-key"

[[sources]]
ref = "piighost/slack-token"

[[sources]]
ref = "piighost/aws-secret-access-key"

[[sources]]
ref = "piighost/azure-storage-key"

[[sources]]
ref = "piighost/sendgrid-api-key"

[[sources]]
ref = "piighost/twilio-sid"

[[sources]]
ref = "piighost/npm-token"

[[sources]]
ref = "piighost/pypi-token"

[[sources]]
ref = "piighost/bearer-token"

[[sources]]
ref = "piighost/certificate-block"

[[sources]]
ref = "piighost/gitlab-token"

[[sources]]
ref = "piighost/mailgun-api-key"

[[sources]]
ref = "piighost/digitalocean-token"

[[sources]]
ref = "piighost/google-oauth-client-id"

[[sources]]
ref = "piighost/ssh-public-key"
