schema_version = 1

[group]
name = "ie"
description = { en = "Ireland: phone, Eircode and the PPS number. Country groups are meant to be used one at a time: a postal code is five digits in several of them, so two in one detector fight for the same span.", fr = "Irlande : téléphone, Eircode et le numéro PPS. Les groupes nationaux sont faits pour être utilisés un à la fois : un code postal fait cinq chiffres dans plusieurs d'entre eux, donc deux dans un même détecteur se disputent le même span." }
tags = ["contact", "government-id", "identity", "ie", "location"]

[[sources]]
ref = "piighost/ie-phone"

[[sources]]
ref = "piighost/ie-eircode"

[[sources]]
ref = "piighost/ie-ppsn"
